<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.boot05web01.mapper.FriendMapper">
    <!--public Friend getFriend(Integer id);-->
    <select id="getFriendById" resultType="com.atguigu.boot05web01.Bean.Friend">
        select * from t_friend where id=#{id}
    </select>
</mapper>